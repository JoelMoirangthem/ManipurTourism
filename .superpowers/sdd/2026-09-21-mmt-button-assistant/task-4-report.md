# Task 4 Report: Trip widget + honest offers on Home

Status: done.

Test summary: `npm run build` compiled successfully and the `travellers` grep returns WIDGET with zero leftover `href="/assistant"` links.

Concerns: none — deviation note: `primaryButtonClass` from the brief's import sketch was dropped (submit is gold per spec, import would be unused); `page.tsx` carries the `travellers` string via an honest caption under the widget since the widget itself lives in `TripWidget.tsx`.
